<% usage = promotion.usage_limit || 0 %>
<% status = ( promotion.expired? || ( promotion.credits_count >= (usage) && usage != 0) ) ? 'inactive' : 'active' %>
<td class="align-center"><span class="state <%= status %>"><%= status %></span></td>
